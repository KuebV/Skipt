d:radius = 0.0
d:circle = 0.0

> "What is the radius of the circle?\n"
< radius

#this is ideally what references and functions will look like
#ref Math::CalculateCircumference(radius) << circle

if (radius >= 5)
{
    > "Great!"
    ref System::exit(0)
}
else
{
    > "Incorrect"
    ref System::exit(1)
}